[module/bluetooth]
type = custom/script
exec = $HOME/.ochaya/scripts/bluetooth.sh

interval = 1

format-background = ${colors.tp-test}
format-foreground = ${colors.purple}
format-padding = 1

click-left  = exec $HOME/.ochaya/scripts/bluetooth_toggle.sh
click-right = exec blueman-manager
