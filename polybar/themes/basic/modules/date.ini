[module/date]
type = internal/date
interval = 1

date = %a %b %d
date-alt = %b %d

label = "%date%"
label-foreground = ${colors.fg}
label-background = ${colors.tp-test}
label-padding = 1

format-prefix = "󰃭"
format-prefix-foreground = ${colors.purple}
format-prefix-background = ${colors.tp-test}

